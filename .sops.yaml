keys:
  - &macbook_pro_m5 age1nzsx242psud20dkle5qqfdnxkw899k9cnte07ljxch7yt5zmtc6sz9hmtl

creation_rules:
  - path_regex: secrets/.*\.yaml$
    key_groups:
      - age:
          - *macbook_pro_m5
  # Whole-document secrets (encrypted as opaque binary blobs, not YAML values).
  # Used for the private `my-voice` Claude Code skill, which quotes real
  # colleagues, clients, and business specifics and must not land in this
  # public repo in plaintext. Edit with `sops secrets/my-voice/<file>.md`.
  - path_regex: secrets/.*\.md$
    key_groups:
      - age:
          - *macbook_pro_m5
