# Testing parameters

- [ ] Command Line Tools Installed
- [ ] Dotfiles copied to home directory

# CLI Tools Installed

1. Open dotfiles repo in chosen base image
1. Run `install.sh`
1. Validate packages are all installed, per Kernel
1. Validate all files are in home directory and symlinked to correct file
1. Run any functional tests
