#!/bin/bash

branches=`git branch --merged | grep -v "master" | grep -v "production" | grep -v $(git rev-parse --abbrev-ref HEAD)`

for branch in $branches; do
  git branch -d $branch
  git push origin :$branch
done

git gc
